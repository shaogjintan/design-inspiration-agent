# design-inspiration-agent (retired)

This was an earlier Next.js prototype for **Forma** (Team Shaken Not Stirred,
`9RGSAV87`, NUS-ISS Show Me Your Agents 2026). The team has moved to
**Streamlit** as the frontend — see [`../roomly-streamlit`](../roomly-streamlit)
for the active project.

Kept here for reference (the Bedrock LLM Gateway wiring in `src/lib/ai/gateway.ts`
may still be useful as a reference for the Streamlit port's own gateway client).
